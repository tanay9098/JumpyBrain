"""Hand notes for the boot files. Imported for side effects."""
from explainers.specific import register

# Filled when a parsed line is too vague. Boot files are short; parser + these cover them.
register("src/main.jsx", {})
register("index.html", {})
