# `index.html`

The source line is on the left of `|`. The explanation of that line is underneath it.

   1 | <!doctype html>
     | In `the top of the file` (index.html): JSX `<element>`. React will create this node when the parent renders. Source: `<!doctype html>`

   2 | <html lang="en">
     | In `the top of the file` (index.html): JSX `<html>`. React will create this node when the parent renders. Source: `<html lang="en">`

   3 |   <head>
     | In `the top of the file` (index.html): JSX `<head>`. React will create this node when the parent renders. Source: `<head>`

   4 |     <meta charset="UTF-8" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta charset="UTF-8" />`

   5 |     <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />`

   6 |     <title>JumpyBrain — Focus better. Do more. Feel calm.</title>
     | In `the top of the file` (index.html): JSX `<title>`. React will create this node when the parent renders. Source: `<title>JumpyBrain — Focus better. Do more. Feel calm.</title>`

   7 |     <meta name="description" content="ADHD-friendly tools and routines that help you stay focused and get things done." />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="description" content="ADHD-friendly tools and routines that help you stay focused and get things done." />`

   8 | 
     | Blank line in `index.html` (the browser page that boots React), inside the top of the file. The parser skips it. It separates the previous statement from the next so the function is readable.

   9 |     <!-- PWA -->
     | In `the top of the file` (index.html): JSX `<element>`. React will create this node when the parent renders. Source: `<!-- PWA -->`

  10 |     <link rel="manifest" href="/manifest.webmanifest" />
     | In `the top of the file` (index.html): JSX `<link>`. React will create this node when the parent renders. Source: `<link rel="manifest" href="/manifest.webmanifest" />`

  11 |     <meta name="theme-color" content="#6366f1" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="theme-color" content="#6366f1" />`

  12 |     <meta name="mobile-web-app-capable" content="yes" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="mobile-web-app-capable" content="yes" />`

  13 |     <meta name="apple-mobile-web-app-capable" content="yes" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="apple-mobile-web-app-capable" content="yes" />`

  14 |     <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />`

  15 |     <meta name="apple-mobile-web-app-title" content="JumpyBrain" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="apple-mobile-web-app-title" content="JumpyBrain" />`

  16 | 
     | Blank line in `index.html` (the browser page that boots React), inside the top of the file. The parser skips it. It separates the previous statement from the next so the function is readable.

  17 |     <!-- Favicon -->
     | In `the top of the file` (index.html): JSX `<element>`. React will create this node when the parent renders. Source: `<!-- Favicon -->`

  18 |     <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
     | In `the top of the file` (index.html): JSX `<link>`. React will create this node when the parent renders. Source: `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`

  19 |     <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
     | In `the top of the file` (index.html): JSX `<link>`. React will create this node when the parent renders. Source: `<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />`

  20 |     <link rel="icon" type="image/png" sizes="16x16" href="/icons/favicon-16.png" />
     | In `the top of the file` (index.html): JSX `<link>`. React will create this node when the parent renders. Source: `<link rel="icon" type="image/png" sizes="16x16" href="/icons/favicon-16.png" />`

  21 |     <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
     | In `the top of the file` (index.html): JSX `<link>`. React will create this node when the parent renders. Source: `<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />`

  22 | 
     | Blank line in `index.html` (the browser page that boots React), inside the top of the file. The parser skips it. It separates the previous statement from the next so the function is readable.

  23 |     <!-- Open Graph -->
     | In `the top of the file` (index.html): JSX `<element>`. React will create this node when the parent renders. Source: `<!-- Open Graph -->`

  24 |     <meta property="og:type" content="website" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta property="og:type" content="website" />`

  25 |     <meta property="og:title" content="JumpyBrain — Focus better. Do more. Feel calm." />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta property="og:title" content="JumpyBrain — Focus better. Do more. Feel calm." />`

  26 |     <meta property="og:description" content="ADHD-friendly tools and routines that help you stay focused and get things done." />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta property="og:description" content="ADHD-friendly tools and routines that help you stay focused and get things done." />`

  27 |     <meta property="og:image" content="/icons/icon-512.png" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta property="og:image" content="/icons/icon-512.png" />`

  28 | 
     | Blank line in `index.html` (the browser page that boots React), inside the top of the file. The parser skips it. It separates the previous statement from the next so the function is readable.

  29 |     <!-- Twitter Card -->
     | In `the top of the file` (index.html): JSX `<element>`. React will create this node when the parent renders. Source: `<!-- Twitter Card -->`

  30 |     <meta name="twitter:card" content="summary" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="twitter:card" content="summary" />`

  31 |     <meta name="twitter:title" content="JumpyBrain" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="twitter:title" content="JumpyBrain" />`

  32 |     <meta name="twitter:description" content="ADHD-friendly focus and productivity tools." />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="twitter:description" content="ADHD-friendly focus and productivity tools." />`

  33 |     <meta name="twitter:image" content="/icons/icon-512.png" />
     | In `the top of the file` (index.html): JSX `<meta>`. React will create this node when the parent renders. Source: `<meta name="twitter:image" content="/icons/icon-512.png" />`

  34 | 
     | Blank line in `index.html` (the browser page that boots React), inside the top of the file. The parser skips it. It separates the previous statement from the next so the function is readable.

  35 |     <script src="https://accounts.google.com/gsi/client" async defer></script>
     | In `the top of the file` (index.html): JSX `<script>`. React will create this node when the parent renders. Source: `<script src="https://accounts.google.com/gsi/client" async defer></script>`

  36 |   </head>
     | In `the top of the file` (index.html): JSX `<head>`. This closes that element. Source: `</head>`

  37 |   <body>
     | In `the top of the file` (index.html): JSX `<body>`. React will create this node when the parent renders. Source: `<body>`

  38 |     <div id="root"></div>
     | In `the top of the file` (index.html): Opens it. A box that groups the elements inside it. Source: `<div id="root"></div>`

  39 |     <script type="module" src="/src/main.jsx"></script>
     | In `the top of the file` (index.html): JSX `<script>`. React will create this node when the parent renders. Source: `<script type="module" src="/src/main.jsx"></script>`

  40 |   </body>
     | In `the top of the file` (index.html): JSX `<body>`. This closes that element. Source: `</body>`

  41 | </html>
     | In `the top of the file` (index.html): JSX `<html>`. This closes that element. Source: `</html>`

